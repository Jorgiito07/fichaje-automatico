name: Fichaje START

on:
  schedule:
    # Lunes a Jueves a las 07:45 España (05:45 UTC)
    - cron: '45 5 * * 1-4'
    # Viernes a las 07:30 España (05:30 UTC)
    - cron: '30 5 * * 5'
  workflow_dispatch:

jobs:
  start_job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install requests
        run: pip install requests
      - name: Ejecutar Start
        run: python fichaje.py start
        env:
          USERNAME: ${{ secrets.USERNAME }}
          PASSWORD: ${{ secrets.PASSWORD }}
